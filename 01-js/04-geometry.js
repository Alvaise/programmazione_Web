// circle
let r = 25;
let diam = r*2;
let circumference = (2*Math.PI)*(r*2);
//let circumference2 = Math.PI*diam;
let area = Math.PI * (Math.pow(r,2));
console.log("The circumference is "+circumference);
console.log("The Area is "+area);

